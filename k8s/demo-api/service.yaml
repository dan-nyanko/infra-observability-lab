apiVersion: v1
kind: Service
metadata:
  name: demo-api
  namespace: observability
spec:
  selector:
    app: demo-api
    version: blue
  ports:
  - port: 80
    targetPort: 5000
  type: LoadBalancer