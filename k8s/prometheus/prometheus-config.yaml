apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: observability
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'infra-demo-api'
        static_configs:
          - targets: ["${PROMETHEUS_TARGET}"]