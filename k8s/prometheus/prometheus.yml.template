global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/prometheus-rules.yml

scrape_configs:
  - job_name: demo-api
    scheme: http
    metrics_path: /metrics
    static_configs:
      - targets:
        - ${PROMETHEUS_TARGET}
